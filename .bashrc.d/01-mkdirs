# vim: ft=bash

HISTORIES=()
HISTORIES+=("bash")
HISTORIES+=("less")
HISTORIES+=("python")
HISTORIES+=("vim")

for h in ${HISTORIES[@]}; do
  if ! [ -d "${XDG_STATE_HOME}/$h" ]; then
    mkdir "${XDG_STATE_HOME}/$h"
  fi
done
